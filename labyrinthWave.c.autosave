#include <stdint.h>
#include <stdbool.h>

#include "labyrinthWave.h"

bool initWave(uint32_t *imageBuff, uint32_t imageH, uint32_t imageW, point2D start, point2D stop)
{
    lab_t (*lab)[imageW] = (lab_t (*)[imageW])imageBuff;
    lab[start.y][]
    while(true) {

    }
}
